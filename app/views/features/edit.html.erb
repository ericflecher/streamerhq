
<% 
	d = @feature.parent_doc_list
	doc = Doc.find(d[0])
	owner = doc.owner_list 

 %>
<% if owner.include?(String(current_user.id)) == true %> 

<h1>Editing feature</h1>

	<%= render 'form' %>

	<%= link_to 'Show', @feature %> |
	<%= link_to 'Back', @feature %>
	
<%else%>

	<div class="alert">
  		<button type="button" class="close" data-dismiss="alert">×</button>
  		<strong>Warning!</strong> Best check yo self, you don't own this feature.
	</div>

<%end%>