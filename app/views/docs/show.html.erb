<!DOCTYPE html>
<html lang="en">

<% u = User.find(@doc.owner_list) %>
<%if @doc.pdoc == 1%>
	<%if current_user.following?(@doc) == true%>
		<%= render "docs/show" %>
	<%else%>
		<% if current_user.id == u[0].id  %> 
			<%= render "docs/show" %>
		<%end%>
	<%end%>
<%else%>
		<%= render "docs/show" %>
<%end%>


</html>