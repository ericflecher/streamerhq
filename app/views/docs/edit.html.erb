<% u = User.find(@doc.owner_list) %>

<% if current_user.id == u[0].id %> 
	
	<h1>Editing doc</h1>
	<%= render 'form' %>

	<%= link_to 'Show', @doc %> |
	<%= link_to 'Back', docs_path %>
	
<%else%>
	
	<div class="alert">
	  <button type="button" class="close" data-dismiss="alert">×</button>
	  <strong>Warning!</strong> Best check yo self, you don't own this doc.
	</div>
	
<%end%>




